# First thing tomorrow (owner-only tasks — nothing here needs code)

In energy order. Smallest first, stop anywhere — everything keeps.

## 1. Push (2 minutes, unlocks everything else)
`git push origin main` (needs your SSH key). Backs up ~6 local commits
and triggers the Netlify deploy. Then hard-refresh the site.

## 2. One line in `.env.test.local` (1 minute)
`TEST_OPENROUTER_MODEL=qwen/qwen3.7-plus` (yours still pins the dead GLM
ID — every key check fails until this changes).

## 3. Sittings, only if you feel like it (no queue — crowns are decided)
- Quality: CROWNED (3.8-max). Value: CROWNED (3.7-plus). Free: vacant.
- Opportunistic only: OpenRouter `qwen/qwen3.8-27b:free` Medium, then
  High, whenever it's unthrottled. No schedule, no obligation.
- Paste transcripts as usual; filing + grading is automated on arrival.

## 4. Verdicts (2 lines each, whenever)
Filed transcripts still read `Grade: TODO` (~15 files in
`docs/eval-sittings/`). Best/worst quotes + one verdict each closes them.
No rush — the mechanical scorecards already hold the counts.

## 5. Golden picks (whenever)
Paste 2–3 best turns (3.7-plus High, 3.8-max High/Medium first in line)
and they get wired as voice exemplars per Phase 7c.

## 6. Small decisions (any order, each under a minute)
- LICENSE file: pick MIT / Apache-2.0 / other — then it's written.
- Name in git history: accept (do nothing) or full rewrite (say so —
  planned together, never alone).
- Together pipe: get a key or drop it from the app.
- Shared-route live test: needs the trial key in Netlify env + deploy.

## 7. Art + texts (waiting on files, not effort)
- Favicon/OG art (one letter in house serif does it).
- Rose: re-convert Mourning (175-byte blank), find Love's Work.

## Explicitly NOT tomorrow
Template scaffold, podcast mode, RAG shard latency, 14B tuning, 30B
presence test, Z.ai (removed), GLM/DeepSeek (parked). All recorded,
none urgent.
