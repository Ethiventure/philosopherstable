logs
*.log
# Playwright MCP run outputs (snapshots, console logs) — local only.
.playwright-mcp/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.venv/
.venv312/
ingest_urls.py.depends

.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.env
.env.*.local
.env.test.local
# Explicit: test-runner keys (test-keys/test-sitting/test-matrix) must never
# reach git, even if the wildcard patterns above are ever narrowed.
# NEVER commit a real API key. Local dev key lives in untracked .env
# (see .env.example). Production key lives in the Netlify dashboard only.

# Netlify local state (netlify dev) — never commit.
.netlify

# Graphify working files (regenerated via `graphify update .`; report + graph stay).
graphify-out/manifest.json
graphify-out/cost.json
graphify-out/cache/
graphify-out/20*/

# RAG pipeline: local SQLite truth stays out; the exported static JSON ships.
data/rag.sqlite

# Local test-probe runs (test-sitting/test-matrix JSONL): machine scribble,
# never committed. Curated transcripts live in docs/eval-sittings/ instead.
data/test-runs/

# Pasted outside-chat transcripts (~47k lines, untracked): open only for
# Rose dossier/diagram receipts, never as project direction, never committed.
docs/User.md
docs/rose-research-chat.md

# Sitting-transcript inbox (owner drops files here; agent files, grades, removes).
# Never committed — processed files move to docs/eval-sittings/ with grades.
docs/eval-sittings/inbox/
